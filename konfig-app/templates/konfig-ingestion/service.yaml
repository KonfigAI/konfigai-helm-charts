apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.components.api.konfig_ingestion.name }}
  name: {{ .Values.components.api.konfig_ingestion.name }}
  namespace: {{ .Values.deployment.namespace }}
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: {{ .Values.components.api.konfig_ingestion.port }}
  selector:
    app: {{ .Values.components.api.konfig_ingestion.name }}
  sessionAffinity: None
  type: ClusterIP