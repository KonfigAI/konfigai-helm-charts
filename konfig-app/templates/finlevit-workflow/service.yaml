apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.components.ui.finlevit_workflow.name }}
  name: {{ .Values.components.ui.finlevit_workflow.name }}
  namespace: {{ .Values.deployment.namespace }}
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: {{ .Values.components.ui.finlevit_workflow.port }}
  selector:
    app: {{ .Values.components.ui.finlevit_workflow.name }}
  sessionAffinity: None
  type: ClusterIP