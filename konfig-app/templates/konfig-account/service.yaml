apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.components.api.konfig_account.name }}
  name: {{ .Values.components.api.konfig_account.name }}
  namespace: {{ .Values.deployment.namespace }}
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: {{ .Values.components.api.konfig_account.port }}
  selector:
    app: {{ .Values.components.api.konfig_account.name }}
  sessionAffinity: None
  type: ClusterIP