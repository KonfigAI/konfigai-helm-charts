apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ .Values.components.ui.finlevit_admin.name }}
  namespace: {{ .Values.deployment.namespace }}
spec:
  gateways:
  - {{ .Values.deployment.gateway }}
  hosts:
  - cluadmin.{{ .Values.deployment.clientName }}.{{ .Values.deployment.domain }}
  http:
  - route:
    - destination:
        host: {{ .Values.components.ui.finlevit_admin.name }}
        port:
          number: 80