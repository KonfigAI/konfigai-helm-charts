apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.components.ui.finlevit_admin.name }}
  name: {{ .Values.components.ui.finlevit_admin.name }}
  namespace: {{ .Values.deployment.namespace }}
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: {{ .Values.components.ui.finlevit_admin.port }}
  selector:
    app: {{ .Values.components.ui.finlevit_admin.name }}
  sessionAffinity: None
  type: ClusterIP
