apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.components.api.konfig_notification.name }}
  name: {{ .Values.components.api.konfig_notification.name }}
  namespace: {{ .Values.deployment.namespace }}
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: {{ .Values.components.api.konfig_notification.port }}
  selector:
    app: {{ .Values.components.api.konfig_notification.name }}
  sessionAffinity: None
  type: ClusterIP